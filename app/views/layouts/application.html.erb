<!DOCTYPE html>
<html>
  <head>
    <title>AlpyneTest</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <p>Current Company: <%= current_tenant.name %></p>

    <%= form_with(url: tenant_switch_path, method: :post, local: true) do |form| %>
    <%= form.select :new_company, ["Propfuel", "Project Solar", "CoFi"] %>
    <%= form.submit "Switch" %>
    <% end%>
    <%= yield %>
  </body>
</html>
